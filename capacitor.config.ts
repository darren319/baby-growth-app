import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.codex.babygrowth",
  appName: "宝宝成长记录",
  webDir: "out",
  backgroundColor: "#FFF8F3",
};

export default config;
